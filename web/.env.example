# Leave unset to run against the built-in demo dataset (src/lib/mock.ts).
# Set both to point the app at a live local Supabase stack:
#   supabase start && supabase status
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=your-local-anon-key
